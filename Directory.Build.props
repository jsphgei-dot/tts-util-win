<Project>
  <PropertyGroup>
    <LangVersion>10</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <Authors>TTS Util Win contributors</Authors>
    <Product>TTS Util Win</Product>
    <Copyright>Copyright (C) 2026</Copyright>
  </PropertyGroup>

  <!-- Version name is semantic; version code is a monotonic integer, one per release. -->
  <PropertyGroup>
    <VersionPrefix>0.10.1</VersionPrefix>
    <VersionSuffix>beta</VersionSuffix>
    <VersionCode>17</VersionCode>
    <AssemblyVersion>0.10.1.0</AssemblyVersion>
    <FileVersion>0.10.1.$(VersionCode)</FileVersion>
  </PropertyGroup>

  <ItemGroup>
    <AssemblyMetadata Include="VersionCode" Value="$(VersionCode)" />
  </ItemGroup>
</Project>
